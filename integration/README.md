Test the headerdb command on a few open source projects.

To use, type:

    make

These tests may be sensible to the platform and software versions they are run
on, don't be panic if manual tweaking is necessary.
